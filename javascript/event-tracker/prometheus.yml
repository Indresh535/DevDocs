global:
  scrape_interval: 2s  # Fetch metrics every 2 seconds

scrape_configs:
  - job_name: "worker_threads"
    static_configs:
      - targets: ["localhost:3001"]  # Worker Threads app

  - job_name: "promise_app"
    static_configs:
      - targets: ["localhost:3002"]  # Promises app
